#include <stdio.h>
#include "main.h"
/**
* add - add two integers.
* @a: number to be added.
* @b: number to be added.
* Return: the value of the result.
*/
int add(int a, int b)
{
int y;
y = a + b;

return (y);
}
