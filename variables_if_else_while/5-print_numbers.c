#include <stdio.h>
/**
* main - Entry point of the program
*
* Description: print all single digit numbers, followed by a line.
*
* Return: Always 0.
**/
int main(void)
{
int x;

for (x = 0; x < 10; x++)
{
printf("%d", x);
}
return (0);
}
